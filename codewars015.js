// Parameters or Edge cases:
// Return: an array with an object 
// Examples:
// Psuedo code: declare an empty array // assign an object and put it into the empty array with .unshift() add at the front of the array or .push() add at the end of the array
items = [], a = {a: 'b', c: 'd'}
items.push(a)
//items.unshift(a) also works since the array is empty adding to the end or the beginning still becomes the only element in the array