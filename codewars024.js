// Parameters or Edge cases: function accepts 1 parameter object
// Return: a string with object
// Examples: {name:"dog",legs:4,color:"white"}
            //and return a string like this:
            //"This white dog has 4 legs."
// Psuedo code: 

function animal(obj){
  return `This ${obj.color} ${obj.name} has ${obj.legs} legs.`;
}
  