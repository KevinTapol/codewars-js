// Parameters or Edge cases: input array elements are integers
// Return: an array of values doubled
// Examples: [1, 2, 3] --> [2, 4, 6]
// Psuedo code: .map() multiplying by 2

//my answer best practices most clever
function maps(x){
   return x.map(e => e*2)
}